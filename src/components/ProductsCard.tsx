import { Text, View } from "react-native";
import { SvgXml } from "react-native-svg";

export const ProductsCard = ({ lists }) => {
  return (
    <View className="pr-[12]">
      {/* <SvgXml className="w-[160px] h-[185px]" xml={props.xml} />
      <Text className="text-[#2D2A2B] text-[16px] leading-[20px] font-medium pt-[10] pb-[2]">{props.name}</Text>
      <Text className="text-[#2D2A2B] text-[13px] leading-[20px] font-light ">{props.price}</Text> */}
    </View>
  );
};
